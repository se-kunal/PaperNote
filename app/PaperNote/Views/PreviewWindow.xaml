<Window x:Class="PaperNote.Views.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="PaperNote" Width="560" Height="620" MinWidth="360" MinHeight="300"
        Icon="../Assets/app.ico"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" FontFamily="{StaticResource UiFont}">
    <Border Background="{DynamicResource PaperBrush}" CornerRadius="10"
            BorderBrush="{DynamicResource LineBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.22" BlurRadius="20" ShadowDepth="4" Direction="270"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="38"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- title strip; drag to move. Shows the previewed file name + a Save action. -->
            <Border Grid.Row="0" Background="{StaticResource AccentBrush}" CornerRadius="10,10,0,0"
                    MouseLeftButtonDown="OnTitleBarDrag">
                <Grid Margin="12,0,6,0">
                    <TextBlock x:Name="TitleText" Text="Preview" Foreground="White" FontSize="12.5"
                               FontWeight="SemiBold" VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="SaveBtn" Click="OnSave" Cursor="Hand" Height="26"
                                Padding="10,0" Margin="0,0,6,0"
                                Background="#33FFFFFF" BorderThickness="0" Foreground="White"
                                Content="Save to PaperNote" FontSize="12" FontWeight="SemiBold"
                                ToolTip="Keep this as a note (Ctrl+S)"/>
                        <Button Click="OnClose" Cursor="Hand" Width="26" Height="26"
                                Background="Transparent" BorderThickness="0" Foreground="White"
                                ToolTip="Close without saving">
                            <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="12"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <wv:WebView2 Grid.Row="1" x:Name="Web"/>
        </Grid>
    </Border>
</Window>
