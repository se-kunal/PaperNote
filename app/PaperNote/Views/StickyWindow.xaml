<Window x:Class="PaperNote.Views.StickyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="PaperNote" Width="380" Height="460" MinWidth="260" MinHeight="220"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" FontFamily="{StaticResource UiFont}">
    <Border Background="{DynamicResource PaperBrush}" CornerRadius="10"
            BorderBrush="{DynamicResource LineBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.22" BlurRadius="20" ShadowDepth="4" Direction="270"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="34"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- title strip (green = "this is a pinned sticky"); drag to move -->
            <Border Grid.Row="0" Background="{StaticResource AccentBrush}" CornerRadius="10,10,0,0"
                    MouseLeftButtonDown="OnTitleBarDrag">
                <Grid Margin="12,0,6,0">
                    <TextBlock x:Name="TitleText" Text="Note" Foreground="White" FontSize="12.5"
                               FontWeight="SemiBold" VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="PinBtn" Click="OnTogglePin" Cursor="Hand" Width="26" Height="26"
                                Background="Transparent" BorderThickness="0" Foreground="White"
                                ToolTip="Keep on top">
                            <TextBlock x:Name="PinGlyph" Text="&#xE840;" FontFamily="Segoe MDL2 Assets" FontSize="13"/>
                        </Button>
                        <Button Click="OnClose" Cursor="Hand" Width="26" Height="26"
                                Background="Transparent" BorderThickness="0" Foreground="White" ToolTip="Close">
                            <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="12"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <wv:WebView2 Grid.Row="1" x:Name="Web"/>
        </Grid>
    </Border>
</Window>
