<UserControl x:Class="PaperNote.Views.NoteListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource ListBgBrush}">
    <Grid Margin="14,16,12,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- header -->
        <Grid Grid.Row="0" Margin="4,0,0,12">
            <TextBlock Text="{Binding CurrentFolderName}" FontSize="19" FontWeight="SemiBold"
                       VerticalAlignment="Center" Foreground="{DynamicResource InkBrush}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <!-- Empty Trash (only in trash) -->
                <Button Content="Empty Trash" Command="{Binding EmptyTrashCommand}" Cursor="Hand"
                        Background="Transparent" BorderThickness="0" FontSize="12" Padding="6,4" Margin="0,0,4,0"
                        Foreground="{DynamicResource InkSecondaryBrush}"
                        Visibility="{Binding IsTrashSelected, Converter={StaticResource BoolToVis}}"/>
                <!-- Import (hidden in trash) -->
                <Button x:Name="ImportBtn" Click="OnImport" Cursor="Hand" ToolTip="Import .txt or .md files"
                        Width="32" Height="32" BorderThickness="0" Margin="0,0,4,0"
                        Background="Transparent" Foreground="{DynamicResource InkSecondaryBrush}"
                        Visibility="{Binding IsTrashSelected, Converter={StaticResource InverseBoolToVis}}">
                    <TextBlock Text="&#xE8E5;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                </Button>
                <!-- New note (hidden in trash) -->
                <Button Width="32" Height="32" Cursor="Hand" Command="{Binding NewNoteCommand}"
                        ToolTip="New note (Ctrl+N)" BorderThickness="0" Foreground="White"
                        Visibility="{Binding IsTrashSelected, Converter={StaticResource InverseBoolToVis}}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" CornerRadius="9" Background="{StaticResource AccentBrush}">
                                <TextBlock Text="+" FontSize="19" HorizontalAlignment="Center"
                                           VerticalAlignment="Center" Margin="0,-2,0,0"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentDeepBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>

        <!-- search -->
        <Border Grid.Row="1" CornerRadius="8" Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource LineBrush}" BorderThickness="1" Margin="0,0,0,10">
            <Grid>
                <TextBox x:Name="SearchBox" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         BorderThickness="0" Background="Transparent" Padding="32,8,10,8"
                         VerticalContentAlignment="Center" Foreground="{DynamicResource InkBrush}"
                         FontSize="13"/>
                <TextBlock Text="&#xE721;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                           Foreground="{DynamicResource InkTertiaryBrush}" Margin="12,0,0,0"
                           VerticalAlignment="Center" IsHitTestVisible="False"/>
                <TextBlock Text="Search" Foreground="{DynamicResource InkTertiaryBrush}"
                           Margin="32,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False"
                           FontSize="13">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SearchText}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>

        <!-- notes -->
        <ListBox Grid.Row="2" x:Name="NoteList" ItemsSource="{Binding Notes}"
                 SelectedItem="{Binding SelectedNote, Mode=TwoWay}"
                 Background="Transparent" BorderThickness="0"
                 PreviewMouseLeftButtonDown="OnNotePreviewMouseDown"
                 PreviewMouseMove="OnNotePreviewMouseMove"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 HorizontalContentAlignment="Stretch">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource AnimatedListItem}">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="bd" CornerRadius="8" Padding="11,10" Background="Transparent"
                                        BorderBrush="Transparent" BorderThickness="1">
                                    <Grid ClipToBounds="True">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="1"/>
                                        </Grid.RowDefinitions>
                                        <ContentPresenter Grid.Row="0"/>
                                        <Border Grid.Row="1" Height="1" Margin="0,9,-11,-9"
                                                Background="{DynamicResource LineBrush}" Opacity="0.55"/>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="{DynamicResource SelectedBrush}"/>
                                        <Setter TargetName="bd" Property="BorderBrush" Value="#00000000"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ListBox}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.InputBindings>
                            <MouseBinding MouseAction="LeftDoubleClick"
                                          Command="{Binding DataContext.BeginRenameNoteCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                          CommandParameter="{Binding}"/>
                        </Grid.InputBindings>
                        <Grid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Rename"
                                          Command="{Binding PlacementTarget.Tag.BeginRenameNoteCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          Visibility="{Binding PlacementTarget.Tag.IsTrashSelected, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource InverseBoolToVis}}"/>
                                <MenuItem Header="Pin / Unpin"
                                          Command="{Binding PlacementTarget.Tag.TogglePinCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          Visibility="{Binding PlacementTarget.Tag.IsTrashSelected, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource InverseBoolToVis}}"/>
                                <MenuItem Header="Lock note" Click="OnLockNote"
                                          Visibility="{Binding PlacementTarget.DataContext.Locked, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource InverseBoolToVis}}"/>
                                <MenuItem Header="Unlock note" Click="OnUnlockNote"
                                          Visibility="{Binding PlacementTarget.DataContext.Locked, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource BoolToVis}}"/>
                                <MenuItem Header="Open as sticky" Click="OnOpenSticky"
                                          Visibility="{Binding PlacementTarget.Tag.IsTrashSelected, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource InverseBoolToVis}}"/>
                                <MenuItem Header="Delete"
                                          Command="{Binding PlacementTarget.Tag.DeleteNoteCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          Visibility="{Binding PlacementTarget.Tag.IsTrashSelected, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource InverseBoolToVis}}"/>
                                <MenuItem Header="Restore"
                                          Command="{Binding PlacementTarget.Tag.RestoreNoteCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          Visibility="{Binding PlacementTarget.Tag.IsTrashSelected, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource BoolToVis}}"/>
                                <MenuItem Header="Delete Forever"
                                          Command="{Binding PlacementTarget.Tag.DeleteNoteCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          Visibility="{Binding PlacementTarget.Tag.IsTrashSelected, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource BoolToVis}}"/>
                            </ContextMenu>
                        </Grid.ContextMenu>

                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal" Margin="0,0,4,0">
                                <TextBlock Text="&#xE718;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                                           Margin="0,0,6,0" VerticalAlignment="Center"
                                           Foreground="{StaticResource AccentDeepBrush}"
                                           Visibility="{Binding Pinned, Converter={StaticResource BoolToVis}}"/>
                                <TextBlock Text="&#xE72E;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                                           Margin="0,0,6,0" VerticalAlignment="Center"
                                           Foreground="{DynamicResource InkTertiaryBrush}"
                                           Visibility="{Binding Locked, Converter={StaticResource BoolToVis}}"/>
                                <TextBlock Text="{Binding Title}" FontSize="13.5" FontWeight="SemiBold"
                                           Foreground="{DynamicResource InkBrush}" TextTrimming="CharacterEllipsis"
                                           Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVis}}"/>
                            </StackPanel>
                            <TextBox Text="{Binding EditTitle, UpdateSourceTrigger=PropertyChanged}"
                                     Tag="note" KeyDown="OnRenameKeyDown" LostFocus="OnRenameCommit"
                                     Loaded="OnRenameBoxLoaded" FontSize="14.5" FontWeight="SemiBold" Padding="0"
                                     BorderThickness="0" Background="White" Foreground="{DynamicResource InkBrush}"
                                     Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"/>
                            <Grid Margin="0,2,4,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding DateLabel}" FontSize="11.5"
                                           FontWeight="SemiBold" Foreground="{DynamicResource InkSecondaryBrush}"
                                           Margin="0,0,7,0"/>
                                <TextBlock Grid.Column="1" Text="{Binding Preview}" FontSize="11.5"
                                           Foreground="{DynamicResource InkTertiaryBrush}"
                                           TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                            </Grid>
                        </StackPanel>

                        <!-- Move to Trash: revealed on card hover; hidden inside Trash -->
                        <Button Grid.Column="1" Width="26" Height="26" Cursor="Hand" ToolTip="Move to Trash"
                                VerticalAlignment="Top" BorderThickness="0" Background="Transparent"
                                Command="{Binding DataContext.DeleteNoteCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                CommandParameter="{Binding}"
                                Visibility="{Binding DataContext.IsTrashSelected, RelativeSource={RelativeSource AncestorType=ListBox}, Converter={StaticResource InverseBoolToVis}}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="bg" Background="Transparent" CornerRadius="6">
                                        <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="13"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Foreground="{DynamicResource InkTertiaryBrush}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bg" Property="Background" Value="#1FE81123"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Opacity" Value="0"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                            <Setter Property="Opacity" Value="1"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
